import { AdvancedMarkerDirective } from './advanced-marker.directive';

describe('AdvancedMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvancedMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
